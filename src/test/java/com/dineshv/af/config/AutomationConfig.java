package com.dineshv.af.config;

public class AutomationConfig {
    // Use this class to define global parameters (defaults) for test automation

    // Selenium default timeout for explicit waits (seconds)
    public static final long DEFAULT_TIMEOUT = 5;
}
